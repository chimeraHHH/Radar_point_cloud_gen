# STDA-F0 pre-freeze audit, round 2

> Date: 2026-08-08  
> Audited protocol SHA-256:
> `e6f23e2f0f989d74c14a7c914b30ea6a9a338aaa5ff5aaf933ee506273aac4b9`  
> Verdict: **NO-FREEZE**  
> Mode: independent hostile read-only audit; no repository files modified

The second audit accepted the route renaming, all-76 cohort, broad target-use
boundary, and narrow scientific claim. It retained 11 executable blockers:

1. replace the underspecified KD-tree spacing check with complete exact pair
   enumeration;
2. freeze an STDA-specific first/later evaluator that supports multiple events
   per VRH cell;
3. restore six `range stratum x first/later` absolute classes;
4. bind a target-free expected candidate-hash manifest and narrow the meaning
   of forbidden target-derived artifacts;
5. define duplicate-mass equivalence and CDF/search pseudocode exactly;
6. pin KNN parameters, support rank, CSR bytes, integer costs, and objective
   overflow checks;
7. independently verify the Hall-deficit inequality;
8. enumerate the shared scientific gate vector used by control comparisons;
9. define cross-process timing, CUDA synchronization, and NVML ceilings;
10. remove circularity from the atomic bundle root and require durable mirroring;
11. state in the top-level question that STDA selects only from one parity-
    packed subset, not freely from all 700k points.

These are protocol defects, not scientific results. No implementation or
real-data STDA run is authorized until they are repaired and a later audit
returns `FREEZE`.
